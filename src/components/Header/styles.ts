import styled from "styled-components"

import headimg from "assets/IMG/cabecalho.svg"

export const Header = styled.header`
    width: 100%;
    display: flex;
    background-image: url("${headimg}");
`